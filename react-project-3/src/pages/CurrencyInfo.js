
import React from 'react';

function CurrencyInfo({ name, code }) {
  return (
    <div>
      <p>Currency Code: ${this.currencyCode};
      Currency Name: ${this.currencyData.name};</p>
      <h2>ss</h2>
    </div>
  );
}

export default CurrencyInfo;

